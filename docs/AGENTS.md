# AGENTS.md

- Use `svg` for diagrams. When creating diagrams, store diagram source as a separate `svg` file.
  Use copilot vision or similar tools to validate `svg` rendering - check for readability and layout issues.
  If the diagram does not render well in markdown, adjust the layout or split it into multiple diagrams if needed.
