# Docs

- in the architecture diagram use also windows named pipes in addition to Unix Domain Sockets, to show that both are supported and can be used in different environments.
- `Exposes Workload API as Unix Domain Socket.` -> `Exposes Workload API as Unix Domain Socket or Windows Named Pipe.` - verify against spire docs that this is correct and supported.
- There are multiple places in docs where only Unix Domain Sockets are mentioned, update those to include Windows Named Pipes as well, if supported by spire.
- Add Azure Key Vault plugin if supported by spire; now there only `aws_mks` is mentioned, in general add azure where aws is mentioned if supported by spire or explicitly state if not supported.
- WorkloadAttestor only mentions `unix, systemd, docker` - verify that windows is not supported and if it is, add it to the list or
  explicitly state that it is not supported.
- There are multiple places in docs where only `unix`, `systemd`, `docker` or `k8s` are mentioned, I need windows explicitly mentioned as well if supported by spire, or explicitly state that it is not supported.
- fix links to scenarios in the documentation `https://miroslav-matejovsky.github.io/scenarios/01-simple/README.md` should be `https://github.com/miroslav-matejovsky/spiffe-spire-demo/tree/main/scenarios/01-simple`, the same for all scenarios.
- Improve Deployment Guide, add diagrams, split into two guides for different environments; Environments to cover:
1. SPIRE SERVER in Azure (linux container) + Business API in Azure (need AGENT as well, linux container might be user here) + SPIRE AGENT (windows service) on Windows VM not connected to domain + Business client (windows service) on the same Windows VM as SPIRE AGENT
2. SPIRE SERVER (windows service) on VM + Business API on the same VM + SPIRE AGENT (windows service) on the same VM + Business client (windows service) on separate VMs on different network segments/domains
- verify everything against spire documentation and update accordingly, especially around supported platforms, plugins, and features. If something is not supported by spire, explicitly state that in the documentation to avoid confusion. Add links to spire documentation where relevant to provide more information and context for readers.