server {
  spire_socket_path = "unix:///tmp/spire-server/private/api.sock"
  http {
    port = 10000
  }
}

plugins {
  DataStore "sql" {
    plugin_data {
      drivername = "sqlite3"
      filename = "/run/spire/data/tornjak.sqlite3"
    }
  }
}
