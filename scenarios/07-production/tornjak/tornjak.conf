server {
  spire_socket_path = "unix:///tmp/spire-server/private/api.sock"
  http {
    enabled = true
    port    = 10000
  }
}

plugins {
  DataStore "sql" {
    plugin_data {
      drivername = "sqlite3"
      filename   = "/tmp/tornjak.sqlite3"
    }
  }
}
