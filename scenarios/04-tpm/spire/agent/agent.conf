agent {
    data_dir = "/opt/spire/data/agent"
    log_level = "INFO"
    log_file = "/opt/spire/logs/spire.log"
    server_address = "spire-server"
    server_port = "8081"
    socket_path = "/opt/spire/sockets/workload_api.sock"
    trust_domain = "mirmat.org"
    insecure_bootstrap = true
}

plugins {
    NodeAttestor "x509pop" {
        plugin_data {
            private_key_path = "/opt/spire/conf/agent/devid-key.pem"
            certificate_path = "/opt/spire/conf/agent/devid-cert.pem"
        }
    }

    KeyManager "disk" {
        plugin_data {
            directory = "/opt/spire/data/agent"
        }
    }

    WorkloadAttestor "unix" {
        plugin_data {}
    }
}
